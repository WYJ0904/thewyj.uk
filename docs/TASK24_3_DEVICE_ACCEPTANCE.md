# Task 24.3 真机验收矩阵（统一最后处理）

状态：**WAITING FOR DEVICE ACCEPTANCE**。以下项目只能由真机确认，尚未执行，一律记为
`WAIVED / UNVALIDATED`，不得当作 PASS。自动化部分（audit / fix / regression / 全量测试 /
PR / CI / merge / Production smoke）见 `docs/TASK24_3_AUDIT.md`。

设备基线：SM-S9360（Android 16，One UI）为既有验收机型；其它机型如有差异请注明。

## T24.3-03 自动入账后通知档案终态（P0）

1. 微信 / 支付宝真实支付一笔，处于「服务端自动入账（本机没有 candidate）」路径。
2. 通知档案条目应显示已入账（confirmed）并带交易号，而不是停在「等待确认记账」。
3. 下拉刷新 / 冷启动 / force stop 后重启 / 切换网络：终态不得被 pending 覆盖。

## T24.3-06 离线捕获 → 联网自动补传（P2）

4. 开飞行模式，制造一条真实支付通知（或银行短信），确认本机显示「待同步」。
5. 关闭飞行模式，**不要**打开通知档案/待核实页面、也不要制造新通知；等待 1–2 分钟。
6. 该笔应自动补传并进入 Finance（通知档案变为已入账/已同步）。

## T24.3-04 无障碍与权限状态一致性（P2）

7. 系统设置里已开启无障碍 → 冷启动 App（不要先打开权限中心）→「我的 → Android 能力」
   与「权限中心」必须一致（不得出现一个说已开启、另一个说未连接）。
8. 在系统设置中关闭无障碍 → 回到 App：两处都应显示未开启；重新开启后应显示已连接。
9. 撤销通知访问 → 回到 App：通知访问显示未开启；重新授权后恢复捕获。

## T24.3-05 更新包完整性（P2）

10. 当前已是最新版（1.3.0/13）：检查更新应显示「当前已是最新版」，不出现可安装包。
11. 若发布 1.3.1：走完 下载 → 校验 → 安装未知应用授权 → 系统安装器 全流程；
    校验失败的场景（哈希不一致）必须在安装前停止并删除文件。

## T24.3-01 / 02 客户端隔离与能力提示（P1/P2）

12. 多账户：账户 A 停在大文件上传中断状态，切到账户 B 并操作，再切回 A：
    A 的待续传队列不得丢失；B 不得看到 A 的文件。
13. 关闭系统「文字转语音」引擎后，离线情况下的朗读入口应提示缺失/不可用，
    不得静默失败；联网时云端 TTS 正常播放。

## 回归范围（既有验收保持有效）

14. Task 24.1/24.2 已通过的真机矩阵（clean install、覆盖升级、reboot、VPN、微信/支付宝
    支付 → pending → confirm、通知点击 cold/warm/kill 路由、文件传输多文件/大文件）如无新
    改动影响的证据，可标记为「沿用既有 PASS」；本轮新增改动影响的路径以上方 1–13 为准。

完成后请把结果（PASS / FAIL + 现象）回填本文件；FAIL 项按 `reproduce → root cause → fix →
regression → commit` 继续处理。
