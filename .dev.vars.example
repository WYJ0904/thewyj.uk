# Copy this file to .dev.vars only when a future cloud route needs a secret.
# Do not put LOCAL_API_BASE or feature flags here; those non-secret values live in wrangler.jsonc.
# EXAMPLE_SECRET=replace-locally-and-never-commit
